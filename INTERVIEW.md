# 面试口述稿 · 高并发直播弹幕（X-Plore）

> 目标：2～3 分钟讲清项目 + 顶住两层追问。  
> 只讲 **弹幕主线**；WaveHub 音乐站是同仓另一项目，见文末。

---

## 60 秒版本（开场必背）

我做了一个**直播弹幕系统**，Go 实现，目标是支撑**百万级 WebSocket 长连接**和多房间实时广播。

先做了**单体**：每台机器管本机连接，用**分片 Hub** 管房间，worker 池批量聚合后广播；跨机用 **Redis Pub/Sub** 实时推，**Kafka** 做持久化和落 **ClickHouse**。Nginx 用一致性哈希做连接亲和。

单机压测大概 **1 万连接、低扇出时 E2E P90 大约 5ms**，零读写错误。

后面发现 Redis Pub/Sub 在集群规模变大时扇出不划算，就按 **Bilibili goim** 思路拆成 **Comet / Logic / Job**：连接层、逻辑层、扇出层独立扩缩，跨机改成 **Kafka 削峰 + Job 定向 RPC 推回 Comet**。服务发现和 RPC 用自研的 **minirpc**（注册中心 + 一致性哈希 + 熔断）。

设计上弹幕**允许丢、允许同房间轻微乱序**，换低延迟和水平扩展。

---

## 2～3 分钟版本（按时间线讲）

### ① 场景与指标（20s）

- 场景：直播间弹幕，读多写多、延迟敏感、可丢。
- 指标：连接数、端到端延迟（P50/P90/P99）、丢弃与错误率，而不是「绝对不丢消息」。

### ② 单体怎么扛住（40s）

1. **连接模型**：一连接两 goroutine（read/write），有界 `sendCh`，满了丢——保发送端不被慢客户端拖死。  
2. **房间索引**：Hub **256 分片锁**，避免全局 `RWMutex` 在高连接下打爆。  
3. **削峰**：上行进大 channel，worker 按「条数 / 时间窗口」批量广播，减 syscall。  
4. **双中间件**：Redis 管实时跨机；Kafka 管历史与多下游；ClickHouse MergeTree 按房间+时间排序。  
5. **正确性细节**：全局 `msg_id` 前端去重；会话令牌 + reauth 防僵尸长连接；AC 敏感词；令牌桶限流。

### ③ 踩过的坑 → 为什么拆微服务（40s）

对照审查和压测（可提 `REVIEW.md`）：

| 问题 | 处理 |
|------|------|
| Prometheus 用 `room_id` 当 label → 基数爆炸 | 去掉高基数标签 |
| Redis 每机反序列化全网消息 | 按 channel/房间短路；演进到 Job 定向推 |
| 历史接口未接 ClickHouse 却返回空 | 可选只读 CH，或明确未启用 |
| 压测工具本身放大延迟/误报读错误 | 分片直方图、关闭态区分、纳秒时间戳 |

**拆分动机一句话**：连接数、CPU 逻辑、扇出吞吐的扩展轴不同，绑在一个进程里会互相拖累；Redis 广播语义也不适合无限加机器。

### ④ goim 链路怎么走（30s）

`客户端 → Comet(WS) → Logic(过滤+msg_id) → Kafka → Job → 各 Comet.PushRoom → 本机房间广播`

- Comet 无该房间则 `delivered=0`，不白干活。  
- 发送者自己的消息也走回路，靠 `msg_id` 去重，和「全网统一扇出」一致。  
- 本地可用 standalone Comet 冒烟，不依赖 Kafka。

### ⑤ 边界与诚实句（20s）

- 同房间跨 worker **可能乱序**，弹幕可接受。  
- 关房/踢人等控制面跨机是已知限制（广播已跨机）。  
- 生产 registry 可换 etcd；单机再往上百万连接要动 fd/内核/连接模型，不是只加业务代码。

---

## 高频追问速答

**Q：为什么不用纯 Kafka 做实时广播？**  
A：Kafka 是日志+消费组模型，延迟和「每台连接机都要收到同一条」的广播语义都不理想；实时用 Redis 或 Job 定向推，Kafka 做持久化与多下游。

**Q：为什么不用纯 Redis？**  
A：无持久化、难回放、难多独立下游；集群 Pub/Sub 扇出成本随节点变差。

**Q：单体里 Redis 到底干什么？单机也要吗？**  
A：只干**多实例实时同步弹幕**（Pub/Sub），不是当 DB/缓存。用户可能被 LB 粘到不同 server，只有本机 `Hub.Broadcast` 会漏人。单机可降级不启 Redis；多机才必需。持久化仍走 Kafka。扩不动时再换成 goim 的 Job 定向推。

**Q：消息会丢吗？**  
A：会。`sendCh` 满丢、限流丢、上行队列满丢。直播弹幕优先实时；要可靠靠 Kafka 落库与业务补偿，不在热路径强一致。

**Q：怎么保证不重复？**  
A：全局 `msg_id` + 前端滑动窗口；Redis/Kafka 双路径或回路广播时去重。

**Q：Hub 为什么要分片？**  
A：房间 map 全局锁在万级连接+频繁进出时竞争明显；分片后锁粒度按 room hash，读写更并行。

**Q：worker 批量会不会加大延迟？**  
A：会有最多约一个时间窗口（如 10ms）的聚合延迟，换吞吐和更少的系统调用；窗口可调。

**Q：和 goim 的差异？**  
A：架构同构 Comet/Logic/Job；通信用自研 minirpc（可讲发现、一致性哈希按 room 打 Logic、熔断），落库用 ClickHouse；单体基线保留做对比。

**Q：一致性哈希用在哪？**  
A：Nginx 侧尽量把同一客户端粘到稳定后端；Logic 侧可按 `roomID` 哈希到固定 Logic，便于房间级限流/聚合。

**Q：压测数字怎么来的？**  
A：自研 loadtest，透传客户端纳秒时间戳算 E2E，HDR 分位；注意压测机与 server 分机，否则高扇出会 CPU 互抢，数字失真。

**Q：生产还缺什么？**  
A：注册中心高可用、鉴权与 token 轮换、控制面跨机、大房间分级广播、更细的多租户限流与可观测、K8s 部署与容量模型。

---

## 三个「故事素材」（问啥都能挂）

1. **安全/会话**：长连接不能只靠一次握手 token——引入限时 session + reauth，到期踢；鉴权比较用恒定时间。  
2. **可扩展踩坑**：Prometheus `room_id` 高基数、Redis 全量反序列化——说明「能跑」和「能扩」不是一回事。  
3. **架构判断**：先单体跑通压测，再按扩展轴拆 goim；能说清**什么时候不该拆**（团队/流量/运维成本）。

---

## 仓库边界（别讲混）

| 讲 | 不讲成「同一个项目」 |
|----|----------------------|
| `server` / `comet` / `logic` / `job` / `core` / `consumer` / `loadtest` | `wavehub`、`wavehub-micro`（音乐分享，Gin/Kratos） |
| `PROJECT.md`、`README.md`、`DESIGN-goim.md`、`REVIEW.md` | `SPRINT-2DAY.md`（WaveHub 冲刺计划） |

若面试官问仓库里为什么有 WaveHub：  
*「同学习仓库里的另一个作品，音乐上传转码；弹幕是另一条高并发长连接故事线。」*

---

## 口述自检清单

- [ ] 不看稿能说完 60 秒版  
- [ ] 能默画：单体（Nginx–Server–Redis–Kafka–CH）与 goim（Comet–Logic–Job–Kafka）各一张  
- [ ] 能解释「为什么允许丢」和「msg_id 去重」  
- [ ] 能报一个真实量级数字（如 10k / P90~5ms），并说明场景（低扇出）  
- [ ] 被问不会的深水区：承认边界 + 回到项目里你真做过的一层  

---

*细节命令与架构图：`PROJECT.md`；压测与调优：`README.md`；拆分契约：`DESIGN-goim.md`。*
