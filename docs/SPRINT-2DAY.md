# 两天面试冲刺方案

> 目标校准：不是"学会"，是**每个话题能用 WaveHub 项目讲 2 分钟 + 顶住两层追问**。
> 原则：一切围绕自己的项目说话。面试官对"我做过"的容忍度远高于"我背过"。
> 每块都有 ✅练习（必做）和 📝作业（做完自查）。做不完就砍后面的，不砍前面的。

---

## 第 1 天：地基 + 单体 + 实战闭环

### 上午 ①：环境跑通（最多 3 小时，到点强制止损）

- ✅ 走完 WSL 五步，终点：`curl localhost:8080/api/v1/tracks` 返回 `{"list":[]}`
- ⏰ **止损规则**：Docker 镜像拉不动卡超过 1 小时 → 把报错发给 Claude，同时先跳到下一块看代码，别把一天耗在环境上
- 面试对应题：「你开发环境是什么？」→ 答：Windows + WSL2，服务跑在 Linux 里和生产一致

### 上午 ②：修越权漏洞（30 分钟，已布置的作业）

- ✅ 改 `wavehub/internal/handler/track.go` 的 CompleteUpload：查库→比对 UserID→403→才放行
- 📝 自查：用两个不同用户的 token 互相调对方的 `/complete`，必须拿到 403
- **这是你面试的王牌故事**，对应题：「说一个你发现/修复过的安全问题」→
  背下这个叙事：*"JWT 中间件只做了认证（你是谁），没人做授权（你能动谁的数据）。任何登录用户
  可以对别人的作品触发操作。我在业务层查库比对资源归属，不匹配返回 403。教训是：认证在中间件，
  授权必须在业务层，因为资源归属是业务数据。"*

### 下午：全流程冒烟 + Git 实战（3 小时）

- ✅ 六连 curl 走通：register → login → 错密码(401) → create(带token) → complete → list/detail
- ✅ Git 全程实练（就用修漏洞这次改动）：
  ```bash
  git switch -c fix/idor-complete-upload   # 开分支
  git add -p                               # 按块暂存，逐块看自己改了什么
  git commit -m "fix: CompleteUpload 增加资源归属校验，防止越权"
  git switch main && git merge fix/idor-complete-upload
  git log --oneline --graph                # 看历史长什么样
  ```
- 📝 作业：把 wavehub 推到自己 GitHub（面试可展示），`git push` 前先 `git remote add`
- 面试对应题：「你的 Git 工作流？」→ 答：功能分支 + Conventional Commits（feat/fix/docs）+
  合回 main；多人协作时 PR + code review。「rebase 和 merge 区别？」→ 一句话：merge 保留
  分叉历史多一个合并节点，rebase 把提交搬到目标分支顶端历史成直线；公共分支不 rebase。

### 晚上：Go/Gin 面试题第一批（2 小时，对着代码答）

每题先自己说一遍，再看括号里的锚点回项目验证：

- Go 为什么适合后端？（编译单二进制、goroutine 轻量并发、部署无依赖——对照 docker-compose 只有中间件没有运行时）
- goroutine 和线程的区别？开销？（KB 级栈 vs MB 级；锚点：CompleteUpload 里 `go service.ProcessAudio`）
- 裸 goroutine 做任务的问题？（进程重启任务丢失、无重试→所以要持久化队列 asynq——你已经答对过这题）
- defer 是干嘛的？（锚点：audio.go 关管道；答"函数返回前执行，常用于释放资源，LIFO 顺序"）
- 闭包是什么？（锚点：auth.go 的 JWT(secret) 返回函数并记住 secret——"刻章机器"那个比喻直接讲）
- Go 错误处理哲学？（error 是返回值不是异常；"出错→处理→return"模式；panic 只留给不可恢复错误）
- Gin 中间件原理？（洋葱模型，c.Next 放行 c.Abort 熔断；锚点：画出 路由→JWT中间件→handler 三站图）
- JWT 原理 + 为什么不用查库？（三段结构、签名=盖章、信任来自数学；缺点：无法主动作废→Redis 黑名单）
- 密码怎么存？（bcrypt 加盐慢哈希；为什么不用 MD5——快=帮黑客；恒定时间比较防侧信道）
- 401 和 403 区别？（没证件 vs 有证件但不是你的房间——用你修的漏洞举例）

📝 睡前作业：写出 60 秒项目介绍并说三遍（模板）：
*"我做了一个音乐分享平台 WaveHub，类似 B 站但主打音频。技术栈 Go+Gin+PostgreSQL+Redis+MinIO。
核心链路是：预签名 URL 直传对象存储、异步队列转码、FFmpeg 提取波形峰值存 JSONB 给前端做可视化。
先做了单体版，又用 Kratos 拆了微服务版做对比，所以我对'什么时候该拆微服务'有实际体感。"*

---

## 第 2 天：微服务 + Linux + 模拟面试

### 上午：微服务版跑通 + 架构内化（3 小时）

- ✅ 三个终端起 user/track/media（`make run-user` 等），走一遍 MICROSERVICES.md 第 5 节冒烟
- ✅ 合上文档，**白纸默画**架构图（三个服务+PG+Redis+MinIO+调用箭头），画错的地方就是没懂的地方
- 面试题（对着 MICROSERVICES.md 校对自己的答案）：
  - 服务怎么拆的？依据是什么？（变化速率 + 资源模型；media 是 CPU 密集独立扩容——这是标准答案）
  - 什么时候该上微服务？（背第 6 节：文件数×3 换独立部署/扩容/故障隔离；"改一处发全站"不可接受时）
  - 服务间怎么通信？什么时候同步什么时候异步？（查作者=马上要结果=gRPC；转码=耗时=队列）
  - 数据库怎么办？能 JOIN 别的服务的表吗？（数据归属纪律；media 必须 gRPC 回写；破坏=分布式单体）
  - 服务发现了解吗？（三条路：静态配置→etcd 注册中心→K8s DNS，各自适用场景）
  - 对外 HTTP 对内 gRPC 为什么？（浏览器只认 HTTP；内部要强类型契约+二进制+连接复用）

### 下午：Linux 实战（2 小时，每条命令都对着自己的服务用）

服务开着，逐条练，练完能脱口而出"这命令干嘛的"：

```bash
ss -tlnp | grep 8080        # 谁在监听端口——排查"端口被占用"
ps aux | grep server        # 找进程
kill <pid>                  # 优雅停止；kill -9 是最后手段(为什么？进程没机会清理)
go run ./cmd/server > app.log 2>&1 &    # 后台跑+日志重定向
tail -f app.log             # 实时看日志——服务端日常动作
grep "401" app.log | wc -l  # 统计——日志分析入门
curl -v localhost:8080/api/v1/tracks    # -v 看完整请求响应头
df -h / free -h / top       # 磁盘/内存/CPU 三件套
```

- 面试对应题：「服务挂了/变慢了怎么排查？」→ 背这条链：*top 看负载 → ps 确认进程活着 →
  ss 看端口在不在听 → tail 日志找报错 → curl 本机验证 → 再查依赖(数据库连得上吗)*
- 📝 作业：故意 kill 掉 postgres 容器再 curl 一次，观察报错长什么样，把它加进你的排查故事里

### 晚上：模拟面试 + 查漏（3 小时）

- ✅ 把两天的题全部**口头**过一遍（不许看答案，说不出的标记出来回炉）
- ✅ 找 Claude 模拟面试：让它扮演面试官连续追问你的项目 30 分钟（追问是最好的查漏方式）
- ✅ 准备三个"讲故事"素材，面试官问啥都能挂靠：
  1. 越权漏洞修复（安全意识 + 认证vs授权）
  2. 裸 goroutine → asynq（可靠性思维 + 队列原理）
  3. 单体 vs 微服务亲手对比（架构判断力——大多数候选人只有背的，你有做的）
- 📝 最后作业：把答不上的题列出来发给 Claude，逐个补

---

## 诚实的边界（面试被问到不会的怎么办）

两天覆盖不到的深水区：GMP 调度细节、GC 原理、channel 底层、K8s。被问到就说：
*"这块我目前只到使用层面，还没深入到实现原理，我的做法是先把项目跑通再逐层往下挖，
比如我最近刚搞明白了 X（挑个你真懂的：闭包/JWT/队列）。"*
——承认边界 + 展示学习路径，远好于硬背露馅。**你的胜负手不在深度，在"每个概念都有自己项目的实例"。**
